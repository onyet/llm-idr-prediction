# Sample .env file for API keys and configuration
# Salin file ini menjadi ".env" dan isi nilai API key Anda.
# Jangan commit file .env yang berisi kunci asli ke repositori.

# OpenAI API key (untuk OpenAIAgent)
OPENAI_API_KEY=

# Groq API key (untuk GroqAgent)
GROQ_API_KEY=

# HuggingFace API key (untuk HuggingFaceAgent). Bisa juga menggunakan HF_TOKEN
HUGGINGFACE_API_KEY=
HF_TOKEN=

# URL base untuk Ollama jika berjalan di host lain (default: http://localhost:11434)
OLLAMA_BASE_URL=http://localhost:11434

# Catatan: Anda juga bisa men-setup variabel lainnya sesuai kebutuhan project.
